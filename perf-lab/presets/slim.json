{
  "_comment": "Enable the SLIM_UI category gate. Pref-driven so the same switch works from .mcp.json --pref, ./mach run --setpref, geckodriver capabilities, and testing/profiles in CI.",

  "browser.slimui.enabled": true
}
